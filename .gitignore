__pycache__/
*.py[cod]
.venv/
*.egg-info/
venv/
.mypy_cache/
.pytest_cache/

# Datos crudos e intermedios (§2): no se versionan
data/raw/
data/interim/

# Caché de descargas (red vial, POIs vía Overpass)
cache/

node_modules/
dist/

# Artefactos derivados de `data/processed`: los reproduce el pipeline y además
# están duplicados en `dashboard/public/data/<ciudad>`, que sí se versiona
# porque es lo que publica GitHub Pages. Versionar los dos doblaría el repo.
data/processed/

# Instantáneas .osm.pbf de Geofabrik (1.3 GB las ocho de Perú)
data/interim/osm/
